# run.yml — defines a single deck training run

deck: UWTempo
version: 20
start_from_version: 20     # null = offline bootstrap, set to resume from checkpoint

generations: 5
games_per_gen: 100
replay_buffer_gens: 10

opponents:
  - deck: Standard-MonoR
    mode: minimax
  - deck: Standard-MonoG
    mode: minimax
  - deck: Standard-MonoB
    mode: minimax
  - deck: Standard-MonoW
    mode: minimax
  - deck: Standard-MonoU
    mode: minimax
training:
  analyze_dataset: true
  generate_plots: true
  eval_previous_model: true

log_level: ACTIONS           # ALL | GAMEPLAY | ACTIONS | ERROR

curriculum: configs/curriculum.yml